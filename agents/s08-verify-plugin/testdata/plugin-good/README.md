# plugin-good

Sample plugin layout. Exercised by `make demo`.

- `SKILL.md` — well-formed front-matter with name, description, triggers.
- `harness` — shell stub that prints help text and a `{"ok":true}` envelope.
- `README.md` — this file (required by check_readme_exists).
